# Playground

This folder is not an npm workspace package.

Use [`examples/auth-api`](../../examples/auth-api) for the token-expiry demo scenario documented in the README.

It does not serve fake metrics.
